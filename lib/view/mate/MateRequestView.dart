import 'package:flutter/material.dart';

class MateRequestView extends StatelessWidget {
  const MateRequestView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('메이트 신청'),
      ),
    );
  }
}
