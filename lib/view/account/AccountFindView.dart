import 'package:flutter/material.dart';

class AccountFindView extends StatelessWidget {
  const AccountFindView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('계정 정보를 잊으셨나요?'),),
    );
  }
}
